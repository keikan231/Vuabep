@model CRS.Web.ViewModels._News.InsertNewsViewModel
@{
    ViewBag.Title = "Thêm tin tức mới";
}
@Html.Partial("_Message")
@if (!User.Identity.IsAuthenticated)
{
    <div class="content-wrapper">
        <h1 class="title">
            Lời nhắn từ Vuabep.vn</h1>
        <div class="boxfullcontent">
            @Html.Partial("_MustLoginMessage")
        </div>
    </div>
}
else if (Model != null)
{
    @Html.ValidationSummary(true, ValidationMessages.Summary)
    <div class="section-headline">
        <div class="section-headline-content">
            <h2>
                Thêm tin tức mới</h2>
        </div>
    </div>
    @Html.Partial("_NewsInputForm", Model)
}