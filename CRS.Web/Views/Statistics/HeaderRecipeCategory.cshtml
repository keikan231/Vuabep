@{
    Layout = null;
}
@model IList<RecipeCategory>
@if (Model != null)
{
    if (Model.Count > 0)
    {
        for (var i = 0; i < Model.Count; i++)
        {
            var item = Model[i];
            <li>@Html.RecipeCategoryLink(item.Name, item.Id, item.Name)</li>
        }
    }
}
